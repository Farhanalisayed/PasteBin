# PasteBin Lite

A lightweight **PasteBin clone** built with **Next.js** and **Upstash Redis**.  
Create and share text snippets with optional **TTL (time-to-live)** and **view limits**.

---

## 🚀 Features

- Create pastes with optional expiration time (**TTL**) and maximum views  
- Serverless Redis storage via **Upstash**  
- No manual database migrations or shell access required  
- Seamless deployment on **Vercel**

---

## 📦 Installation

Clone the repository and install dependencies:

```bash
git clone https://github.com/Farhanalisayed/PasteBin.git
cd PasteBin
npm install
